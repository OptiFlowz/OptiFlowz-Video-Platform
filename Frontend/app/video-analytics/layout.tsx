import { createPageMetadata } from "~/metadata";
export const metadata = createPageMetadata({ title: "Video analytics", description: "Review detailed performance and engagement for an OptiFlowz video.", path: "/video-analytics", noIndex: true });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

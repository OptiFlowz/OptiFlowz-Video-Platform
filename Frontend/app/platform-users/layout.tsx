import { createPageMetadata } from "~/metadata";
export const metadata = createPageMetadata({ title: "Platform users", description: "Manage OptiFlowz platform users and access.", path: "/platform-users", noIndex: true });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

import { createPageMetadata } from "~/metadata";
export const metadata = createPageMetadata({ title: "Channel analytics", description: "Review channel performance and audience insights.", path: "/channel-analytics", noIndex: true });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

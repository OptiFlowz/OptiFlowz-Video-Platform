import { createPageMetadata } from "~/metadata";
export const metadata = createPageMetadata({ title: "UEMS reading list", description: "Access the UEMS reading list on OptiFlowz.", path: "/uems-reading-list", noIndex: true });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
